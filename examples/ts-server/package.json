{
  "name": "@bgql/example-ts-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "schema": "cargo run -q --manifest-path ../../Cargo.toml -p bgql_cli -- build schema/mod.bgql -o dist/schema.graphql",
    "codegen": "cargo run -q --manifest-path ../../Cargo.toml -p bgql_cli -- codegen --lang typescript schema/mod.bgql -o src/generated/types.ts",
    "dev": "bun run schema && bun --watch src/server.ts",
    "build": "bun run schema && tsdown",
    "start": "bun dist/server.js",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@bgql/server": "workspace:*",
    "@bgql/client": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@typescript/native-preview": "latest",
    "tsdown": "latest",
    "unplugin-macros": "latest"
  }
}
