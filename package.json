{
  "name": "bgql",
  "private": true,
  "type": "module",
  "workspaces": [
    "sdk/typescript",
    "npm/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun run --filter '*' test"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
